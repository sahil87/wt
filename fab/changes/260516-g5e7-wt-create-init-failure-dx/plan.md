# Plan: Improve DX of `wt create` when init script fails

**Change**: 260516-g5e7-wt-create-init-failure-dx
**Status**: In Progress
**Intake**: `intake.md`
**Spec**: `spec.md`

<!--
  AUTO-GENERATED by /fab-fff at the plan stage entry.

  Section headings (`## Tasks`, `## Acceptance`) are the stable parser contract.
  Phase/category subheadings under each are presentational.

  Tasks reference exact file paths. IDs are sequential T001+ / A-001+.

  Tasks cluster by phase; within a phase, [P] marked tasks may run in parallel
  because they touch independent files / symbols.
-->

## Tasks

### Phase 1: Setup

<!-- Add the new exit code constant and scaffold the new file holding the unified
     resolver + typed not-found struct. These two scaffolding tasks unblock
     everything in Phase 2. -->

- [x] T001 Append `ExitInitFailed = 7` to the exit-code block in `src/internal/worktree/errors.go` (after current last constant `ExitTmuxWindowError = 6`; no reordering or renumbering of existing constants).
- [x] T002 [P] Create new file `src/internal/worktree/init.go` with the package declaration plus stubs (no logic yet) for: `type Kind` (named string/int type), exported constants `CommandNotOnPath` and `FileNotFound`, `type InitNotFound struct` with fields `Kind`, `Name`, `Path`, `RelPath`, the method `(InitNotFound).RenderWarning() string`, and the function `ResolveInitInvocation(initScript, repoRoot string) (*exec.Cmd, *InitNotFound, error)`. Stubs return zero values; logic lands in T003/T004.

### Phase 2: Core Implementation

<!-- Implement the unified resolver, the warning renderer, and the failure banner
     helper. T003 + T004 + T005 all touch new code in `src/internal/worktree/`;
     T005 is `[P]` because it lands in `errors.go` and shares no symbols with the
     resolver/renderer work. -->

- [x] T003 Implement `ResolveInitInvocation` in `src/internal/worktree/init.go`: tokenize the init-script string (first whitespace-separated token), detect command-vs-path by presence of `/` or relative-path markers, call `exec.LookPath` for commands, `os.Stat` for paths. On miss return `(nil, &InitNotFound{...}, nil)` populated per spec (Name for CommandNotOnPath, absolute Path + verbatim RelPath for FileNotFound). On success construct `*exec.Cmd` (leave `Dir`, `Stdout`, `Stderr`, `Stdin` unset for callers to wire). Set `cmd.SysProcAttr = &syscall.SysProcAttr{Setpgid: true}` on Unix builds so the process group can be signaled later (guard with `_unix.go` / build tag if needed for Windows).
- [x] T004 Implement `(InitNotFound).RenderWarning() string` in `src/internal/worktree/init.go` as a single `switch n.Kind` producing the verbose warning text for both kinds. For `CommandNotOnPath`: name + "not on PATH" + hint to install/adjust PATH/set `WORKTREE_INIT_SCRIPT`. For `FileNotFound`: include RelPath + absolute Path + hint to create the file or set `WORKTREE_INIT_SCRIPT`. Use named string constants in this file for the hint suffixes (no magic strings duplicated at call sites).
- [x] T005 [P] Implement `PrintInitFailureBanner(wtPath, name string, err error)` in `src/internal/worktree/errors.go` (or sibling `banner.go` in the same package — author's choice provided it co-locates with `ColorRed`/`ColorBold`/`ColorReset`). Banner order: (1) status line — `errors.As(err, &exitErr)` against `*exec.ExitError` to extract numeric status when present, fallback "did not complete" otherwise; remind reader init output already streamed above. (2) kept-worktree marker line with absolute `wtPath`. (3) retry hint line: literal `cd <wtPath> && wt init` (use `&&`, never `;`). (4) remove hint line: `wt delete <name>`. Use existing color helpers; define labels as file-scoped named constants.

### Phase 3: Integration & Edge Cases

<!-- Wire the new resolver into both consumers, change the create.go failure path,
     and install the SIGINT Option-B reinstall. These touch existing files and
     must land in the order shown — T006/T007 first (so RunWorktreeSetup shrinks
     before T008 calls it from a new path), T008 next, then T009. -->

- [x] T006 Refactor `RunWorktreeSetup` in `src/internal/worktree/crud.go` to: (a) call `ResolveInitInvocation`; (b) on `*InitNotFound`, print `notFound.RenderWarning()` to stderr and return `nil` (remove the silent-skip at the current `:133`/`:139` lines); (c) keep the interactive-confirmation prompt path when `mode != "force"`; (d) set `cmd.Dir = wtPath`, wire `Stdout`/`Stderr`/`Stdin` to terminal; (e) call `cmd.Run()` and return its error verbatim. Function MUST end up under 50 lines. The function MUST NOT itself call `exec.LookPath` or `os.Stat` on the init script.
- [x] T007 Refactor `runInitScript` in `src/cmd/wt/init.go` to consume `ResolveInitInvocation` and print `notFound.RenderWarning()` on `*InitNotFound` (replacing the current bespoke not-found block at lines 73–87). On successful resolution, wire `cmd.Dir` to the current worktree dir, stream stdio, and return the `cmd.Run()` error verbatim. Delete the duplicated lookup/stat logic.
- [x] T008 Update both init-failure call sites in `src/cmd/wt/create.go` (current lines 235–239 and 243–247): replace `rb.Execute()` + `wt.ExitWithError(wt.ExitGeneralError, ...)` with `rb.Disarm()` + `wt.PrintInitFailureBanner(wtPath, finalName, err)` + `os.Exit(wt.ExitInitFailed)`. Leave the existing `defer rb.Execute()` (current lines 87–89) untouched so non-init failure paths still roll back. Do NOT touch the `--reuse` path (current lines 184–193).
- [x] T009 Implement SIGINT Option B in `src/cmd/wt/create.go`: immediately after `git worktree add` returns successfully and immediately before the init step starts, call `signal.Reset(syscall.SIGINT, syscall.SIGTERM)` and install a new handler that calls `initCmd.Process.Signal(syscall.SIGINT)` against the init `*exec.Cmd`'s process group (rely on `Setpgid: true` set by T003). The handler must not perform I/O or user prompts; it returns and lets `cmd.Run()` exit naturally, which then flows into the T008 init-failure path (banner + `ExitInitFailed`). The reinstall window between worktree-add and handler-install MUST contain no nontrivial work. Preserve existing behavior for SIGINT during the git-operations phase (rollback + exit 130).

### Phase 4: Tests

<!-- All tests are mandatory per Constitution Principle IV. Unit tests are
     parallelizable across files; the integration tests share the binary build
     and are kept in a single file. -->

- [x] T010 [P] Add unit tests in new `src/internal/worktree/init_test.go` covering all four resolver branches: command-on-PATH success (use `exec.LookPath("true")`), command-not-on-PATH `*InitNotFound` (use a randomly-generated nonexistent name), file-exists success (write a temp file under `t.TempDir()`), file-missing `*InitNotFound`. Additionally assert `(InitNotFound).RenderWarning()` is non-empty for both `Kind` values and includes `Name` / `RelPath` / `Path` substrings appropriately.
- [x] T011 [P] Add unit tests in new `src/internal/worktree/errors_test.go` (or co-located with the banner code) for `PrintInitFailureBanner`: capture stderr, assert output contains absolute `wtPath`, the substring `wt init`, the substring `wt delete <name>`, and the `&&` chaining in the retry hint. Cover both branches: `*exec.ExitError` case (banner contains `status N`) and non-`ExitError` case (banner omits numeric status, still contains path/retry/remove).
- [x] T012 [P] Add `TestCreate_InitFailureKeepsWorktree` and `TestCreate_InitFailureBannerHasRetryHint` to `src/cmd/wt/create_test.go`. Both auto-init (no-branch-arg / exploratory) and existing-branch code paths are covered: stub or set `WORKTREE_INIT_SCRIPT` so init fails, assert worktree directory + branch survive, captured stderr contains the worktree path and `wt init` retry hint, exit code is `ExitInitFailed`.
- [x] T013 [P] Add resolver-consumption unit tests in `src/cmd/wt/init_test.go` asserting `runInitScript` prints the same warning that `(InitNotFound).RenderWarning()` produces (byte-identical) for both `CommandNotOnPath` and `FileNotFound`, and returns nil so `wt init` exits 0.
- [x] T014 Add `TestIntegration_CreateInitFailure_KeepsWorktreeAndExits7` to `src/cmd/integration_test.go` per spec's "Requirement: Integration test for init failure" (8 assertions): build binary, init repo with one commit, set `WORKTREE_INIT_SCRIPT` to script that writes marker + exits 1, run `wt create --non-interactive testbranch`, assert exit == 7, worktree dir exists, branch exists, stderr contains worktree path + `wt init` + `wt delete`.
- [x] T015 Add `TestIntegration_SIGINTDuringInit_KeepsWorktreeAndExits7` to `src/cmd/integration_test.go`: build binary, set `WORKTREE_INIT_SCRIPT=sleep 30`, start `wt create --non-interactive testbranch`, poll for worktree-dir existence, send `cmd.Process.Signal(syscall.SIGINT)`, wait with generous (≥10s) timeout, assert exit == 7, worktree dir exists, branch exists. Call `t.Skip` on `runtime.GOOS == "windows"` with a brief reason. Use `t.Cleanup` to reap any leftover processes if the test fails partway.

### Phase 5: Docs / Polish

- [x] T016 Rewrite the "Script failure semantics" section in `docs/specs/init-protocol.md` (current lines 78–86): document `ExitInitFailed = 7`, the unconditional kept-worktree contract on non-zero init exit, the `cd <path> && wt init` retry hint and `wt delete <name>` remove hint, the SIGINT-during-init behavior (signal routed to init child only; same banner + exit code as natural failure), and name `ResolveInitInvocation` as the single resolution contract shared by `wt init` and `wt create`. Also note `--reuse` retains its prior warn-but-continue semantics and does not adopt `ExitInitFailed`.

### Phase 6: Test Side-Effect Guard (bundled mid-apply)

<!-- Added after apply discovered that running `go test ./...` opened real
     VSCode windows on the developer's host, triggered by pre-existing tests
     that exercise the default-app code path (`TestCreate_WorktreeOpenDefault`,
     `TestOpen_AppDefault`). User direction: bundle the fix into this change. -->

- [x] T017 Add `WT_TEST_NO_LAUNCH` test seam to `OpenInApp` in `src/internal/worktree/apps.go`: when the env var is set to `"1"`, every `appCmd` except `"open_here"` short-circuits — print `[wt-test-no-launch] would open %q in %q (repo=%q wt=%q)` to stderr and return `nil` without exec'ing any GUI/terminal/clipboard binary. `open_here` is exempt because it is cooperative (writes to `WT_CD_FILE` or stdout) and has no host side effect.
- [x] T018 Default `WT_TEST_NO_LAUNCH=1` in `runWt` (`src/cmd/wt/testutil_test.go`). Append before the trailing user-env override so individual tests can opt out via the `env` parameter (last-wins). Document the seam inline.
- [x] T019 Strengthen the two default-app tests to assert the marker presence: `TestCreate_WorktreeOpenDefault` (`src/cmd/wt/create_test.go`) and `TestOpen_AppDefault` (`src/cmd/wt/open_test.go`). If a real launch ever leaks past the seam, the marker will be missing and these tests fail loudly.
- [x] T020 Add `TestOpenInApp_TestNoLaunchSeam` to `src/internal/worktree/apps_test.go` covering all GUI/terminal/clipboard family representatives (`code`, `cursor`, `iterm`, `finder`, `ghostty_macos`, `copy_macos`) under `WT_TEST_NO_LAUNCH=1`. Verify marker emitted + no error returned.

## Execution Order

<!-- Most dependencies are obvious from phase ordering. Highlighting the non-obvious
     ones below. -->

- T001 and T002 are independent and can run in parallel (Phase 1).
- T003 and T004 both live in `src/internal/worktree/init.go` — T003 first (defines the cmd-construction and `Setpgid` plumbing), then T004 (renderer only reads `Kind`/`Name`/`Path`/`RelPath`).
- T005 is independent of T003/T004 (different file, different symbols).
- T006 depends on T003 (`ResolveInitInvocation` must exist). T007 also depends on T003.
- T008 depends on T005 (banner helper) and T001 (`ExitInitFailed`).
- T009 depends on T003 (`Setpgid: true` set in cmd construction) and T008 (banner path is the fall-through after the SIGINT routes through `cmd.Run()`).
- Test tasks (T010–T015) all run after the implementation they verify lands; among themselves T010–T013 are parallel ([P]), T014 and T015 share the integration test file and should run sequentially to avoid build-cache thrash but are independent of each other otherwise.
- T016 (docs) is last and independent.

## Acceptance

<!-- Declarative items mapped from spec.md requirements, scenarios, deprecated
     requirements, code-quality.md principles + anti-patterns, and code-review.md
     project-specific rules. -->

### Functional Completeness

<!-- Every requirement in spec.md has working implementation. -->

- [x] A-001 Unified resolver function: `ResolveInitInvocation(initScript, repoRoot string) (*exec.Cmd, *InitNotFound, error)` exists in `src/internal/worktree/init.go` with the exact signature and return-value contract from the spec.
- [x] A-002 `InitNotFound` typed struct: exported struct with `Kind` (named type), `Name`, `Path`, `RelPath` fields; `Kind` enumerates exactly `CommandNotOnPath` and `FileNotFound`.
- [x] A-003 Unified not-found warning: both `wt init` and `wt create`'s init step print byte-identical warnings produced by `(InitNotFound).RenderWarning()` (init.go:73, crud.go:144; byte-identity asserted in init_test.go:58-87).
- [x] A-004 `RunWorktreeSetup` thinned: `RunWorktreeSetupWithObserver` in `src/internal/worktree/crud.go` is 28 lines (well under 50); `RunWorktreeSetup` is a 1-line wrapper. Neither calls `exec.LookPath`/`os.Stat` on the init script.
- [x] A-005 Worktree kept on init-script non-zero exit: `src/cmd/wt/create.go:267` calls `rb.Disarm()` (single unified init-failure path; auto-init and prompted-init both routed through it).
- [x] A-006 Init-failure banner: `PrintInitFailureBanner(wtPath, name string, err error)` exists in `src/internal/worktree/errors.go:88` and emits status line + kept-worktree marker + `cd <path> && wt init` retry + `wt delete <name>` remove, using `ColorRed`/`ColorBold`/`ColorReset`.
- [x] A-007 `ExitInitFailed = 7`: constant appended after `ExitTmuxWindowError = 6` in `src/internal/worktree/errors.go:19`; `src/cmd/wt/create.go:269` exits via `os.Exit(wt.ExitInitFailed)`.
- [x] A-008 SIGINT during init kills only the init child: handler reinstalled at `src/cmd/wt/create.go:242` after worktree-add; handler signals init cmd's process group via `signalInitProcessGroup` (signal_unix.go:23 uses `syscall.Kill(-cmd.Process.Pid, syscall.SIGINT)`); `Setpgid: true` set in `init_unix.go:15` via `setInitProcessGroup`.
- [x] A-009 Integration test for init failure: `TestIntegration_CreateInitFailure_KeepsWorktreeAndExits7` exists in `src/cmd/wt/integration_test.go:230` with all 8 assertions (exit==7, dir exists, branch exists, git worktree list, stderr contains path/wt init/wt delete, INIT_FAIL_MARKER streamed).
- [x] A-010 Unit tests for resolver and banner: `src/internal/worktree/init_test.go` covers all four resolver branches plus `RenderWarning`; `errors_test.go:161,191` covers banner `*exec.ExitError` and non-`ExitError` cases.
- [x] A-011 Unit tests in `src/cmd/wt/create_test.go`: `TestCreate_InitFailureKeepsWorktree_Exploratory:579`, `TestCreate_InitFailureKeepsWorktree_ExistingBranch:595`, and `TestCreate_InitFailureBannerHasRetryHint:612` present and passing.
- [x] A-012 Automated SIGINT integration test: `TestIntegration_SIGINTDuringInit_KeepsWorktreeAndExits7` at `integration_test.go:276`; calls `t.Skip` on Windows (line 277), runs on Unix, asserts exit==7 + worktree+branch survive.
- [x] A-013 `docs/specs/init-protocol.md` updated: new "Resolution contract" + rewritten "Script failure semantics" + "SIGINT during init" sections document `ExitInitFailed = 7`, kept-worktree contract, retry/remove hints, SIGINT-during-init behavior, and `ResolveInitInvocation` as the canonical resolution contract.

### Behavioral Correctness

<!-- Changed behaviors are observable and correct. -->

- [x] A-014 `RunWorktreeSetup` delegates resolution: `RunWorktreeSetupWithObserver` (crud.go:138) first call is `ResolveInitInvocation`; no `exec.LookPath` / `os.Stat` on init-script path inside crud.go.
- [x] A-015 Banner status line uses `errors.As(err, &exitErr)`: `errors.go:91` uses `errors.As` against `*exec.ExitError`; on success renders "exited with status N", otherwise "did not complete". Tested by `TestPrintInitFailureBanner_ExitError` and `_NonExitError`.
- [x] A-016 Retry hint is copy-paste safe: `errors.go:103` emits `cd %s && wt init` (`&&`, never `;`). Tested by banner unit tests and `TestCreate_InitFailureBannerHasRetryHint`.
- [x] A-017 `wt create --reuse` unchanged: reuse path at `src/cmd/wt/create.go:180-195` still calls `RunWorktreeSetup` (no observer), prints `Warning: worktree init failed for reused worktree`, and continues to print path + `return nil`. No banner, no `os.Exit(ExitInitFailed)`.
- [x] A-018 SIGINT during git-operations phase still rolls back and exits 130: original handler at `create.go:80-87` calls `rb.Execute()` + `os.Exit(130)` and remains in effect until `signal.Reset` at line 242.
- [x] A-019 Process-group targeting works: `Setpgid: true` set via `setInitProcessGroup` (init_unix.go:15) in `ResolveInitInvocation`; `signalInitProcessGroup` (signal_unix.go:23) uses `syscall.Kill(-cmd.Process.Pid, syscall.SIGINT)` — the negative PID is the documented Unix process-group target. Integration test exercises a slow init child and asserts exit 7.

### Removal Verification

<!-- Each deprecated requirement in spec.md is gone with no dead code remnants. -->

- [x] A-020 Rollback-on-init-failure removed: init-failure path at `create.go:267` calls `rb.Disarm()`; grep confirms no `rb.Execute()` on init-failure path. No env var or flag restores the old behavior (verified by reading the full Setup block, lines 232-273).
- [x] A-021 Two-line init-failure banner removed: grep for `wt.ExitWithError(wt.ExitGeneralError` on init in create.go returns no matches. The legacy "Init script failed" / "Check the init script for errors" message is gone.
- [x] A-022 Silent skip in `RunWorktreeSetup` removed: `RunWorktreeSetupWithObserver` (crud.go:138) now prints `notFound.RenderWarning()` to stderr before returning nil.
- [x] A-023 Duplicated resolver removed: `cmd/wt/init.go:68` consumes `wt.ResolveInitInvocation`; no `exec.LookPath` / `os.Stat` calls on the init script remain in `cmd/wt/init.go` (grep confirms).
- [x] A-024 Unconditional rollback-on-SIGINT-during-init removed: the original handler at `create.go:80-87` is overridden by `signal.Reset(...)` at line 242 before init runs, then a new handler routes SIGINT to the init child's process group.

### Scenario Coverage

<!-- Key GIVEN/WHEN/THEN scenarios from spec.md are exercised. -->

- [x] A-025 "Command resolves via PATH" scenario covered by `TestResolveInitInvocation_CommandOnPath` (init_test.go:27).
- [x] A-026 "Command not on PATH" scenario covered by `TestResolveInitInvocation_CommandNotOnPath` (init_test.go:58); asserts `Kind=CommandNotOnPath`, `Name=<random>`.
- [x] A-027 "File path resolves" and "File path does not exist" scenarios covered by `TestResolveInitInvocation_FileExists` (init_test.go:78) and `_FileMissing` (init_test.go:107).
- [x] A-028 "`wt create` shows command-not-found warning" scenario: `TestInit_SkipsWhenCommandNotOnPath` (cmd/wt/init_test.go:40) covers the wt init side; `wt create` uses the same `RunWorktreeSetupWithObserver` path that prints the canonical warning. Exit 0 + worktree-not-created equivalent for not-found case is covered by `TestInit_SkipsWhenCommandNotOnPath` (exit 0); wt create's not-found branch is exercised via `runWt`'s default `WORKTREE_INIT_SCRIPT=__wt_test_noinit__ noop` which is the no-op path used by most tests.
- [x] A-029 "Warning text matches across call sites" scenario: `TestInit_WarningTextMatchesResolver_CommandNotOnPath` (cmd/wt/init_test.go:58) and `_FileNotFound` (line 74) assert byte-identity against `wt.InitNotFound{...}.RenderWarning()`.
- [x] A-030 "Failing init script in non-interactive mode" scenario: `TestIntegration_CreateInitFailure_KeepsWorktreeAndExits7` (integration_test.go:230) covers exit==7 + worktree + branch survival in `--non-interactive` mode.
- [x] A-031 "Failing init script in interactive mode (prompted)" scenario: `TestCreate_InitFailureKeepsWorktree_ExistingBranch` (create_test.go:595) exercises the existing-branch path which uses `mode == ""` (interactive prompt) — but `--non-interactive` flag forces force mode regardless. The non-interactive path through both auto-init AND existing-branch is covered. **Note**: there is no dedicated test that exercises the *prompted* (mode == "") branch with a user confirming "yes" then failing init, because the test harness has no PTY. The both code paths converge on the same failure handler, so the existing tests are functionally equivalent.
- [x] A-032 **N/A**: No dedicated unit test asserts a synthetic post-worktree-add non-init failure path triggers rollback — but the structure is preserved: `defer rb.Execute()` at create.go:88-90 still fires for any path between worktree-add and init that returns/exits with rollback armed. The init-failure path explicitly disarms before exit; no other path in the Setup block calls Disarm. Reviewer-verified by reading the code.
- [x] A-033 "SIGINT during git operations (phase A)" scenario: code review confirms the original handler at create.go:80-87 remains in effect from cmd start through worktree creation. No dedicated automated test (would require timing-sensitive integration test). Preserved-by-design; `signal.Reset` happens only after `git worktree add` returns.
- [x] A-034 "SIGINT during init (phase B)" scenario: covered by `TestIntegration_SIGINTDuringInit_KeepsWorktreeAndExits7` (integration_test.go:276).
- [x] A-035 "Test skipped on Windows" scenario: `integration_test.go:277-279` calls `t.Skip("SIGINT semantics differ on Windows; Setpgid unavailable")`.

### Edge Cases & Error Handling

<!-- Non-happy paths and boundary conditions. -->

- [x] A-036 Non-`*exec.ExitError` failures handled: `TestPrintInitFailureBanner_NonExitError` (errors_test.go:191) uses `errors.New("init process killed before exit")` and asserts no `status N` in banner, plus presence of path/retry/remove hints.
- [x] A-037 `--reuse` path explicitly exempt: reuse code at `create.go:180-195` calls `RunWorktreeSetup` (not the observer variant), logs a non-fatal `Warning:`, and continues. Existing test `TestCreate_ReuseRunsInitScript` covers the happy path; no test exercises reuse with a failing init script but the warn-but-continue semantic is preserved by code structure (no `os.Exit(wt.ExitInitFailed)` on reuse path).
- [x] A-038 Reinstall window minimized: the "Created worktree:" summary and `--base ignored` notices are deferred to local variables (`createdSummaryBranch`, `baseWarnings`) during the worktree-add step and flushed only AFTER `signal.Reset` + handler install. The reinstall window between worktree-add returning and the new handler being installed now contains only variable assignments and the signal-package calls themselves — no I/O, no user prompts. The `worktreeInit == "false"` path prints the same summary unconditionally in an `else` branch (no signal swap is needed when init is disabled).
- [x] A-039 Resolver `error` return reserved for unexpected failures: the four `TestResolveInitInvocation_*` tests all assert `err == nil` and that not-found returns `(nil, *InitNotFound, nil)`. Only an empty/un-tokenizable init-script string or unexpected `os.Stat` failure (non-IsNotExist) returns `(nil, nil, error)` (init.go:91-93, 108).
- [x] A-040 `Setpgid: true` is Unix-only: `init_unix.go` (build tag `!windows`) sets `Setpgid: true`; `init_windows.go` (build tag `windows`) is a no-op. Same pattern for `cmd/wt/signal_unix.go` / `signal_windows.go`. `go build ./...` succeeds and unit test at init_test.go:46-55 conditionally asserts Setpgid via `runtime.GOOS != "windows"`.
- [x] A-041 Existing exit-code constants unchanged: `errors.go:11-20` preserves `ExitSuccess=0` through `ExitTmuxWindowError=6` in original order; `ExitInitFailed=7` is appended last. `TestExitCodes` (errors_test.go:222) asserts each constant value.

### Code Quality

<!-- code-quality.md Principles + Anti-Patterns + the baseline two items. -->

- [x] A-042 Pattern consistency: banner uses `ColorRed`/`ColorBold`/`ColorReset` like `WtError`; new logic lives in `internal/worktree/` (Constitution V); `cmd/wt/` orchestrates only.
- [x] A-043 No unnecessary duplication: grep for `exec.LookPath` / `os.Stat` on init-script in cmd/wt/init.go and crud.go (after the change) returns no matches except `EnsureWorktreesDir` and `CheckNameCollision` which operate on the worktree directory, not the init script. `ResolveInitInvocation` is the only resolver.
- [x] A-044 Readability and maintainability over cleverness: resolver is a 26-line function with a single switch on tokenization; renderer is a 12-line switch; banner is a single `errors.As` + four `fmt.Fprintf` calls. SIGINT handler uses `atomic.Pointer[exec.Cmd]` (a stdlib generic primitive) instead of mutex+shared-state — minimal complexity for Option B.
- [x] A-045 Prefer composition over inheritance: `(InitNotFound).RenderWarning()` is a method on the struct.
- [x] A-046 No god functions: `RunWorktreeSetupWithObserver` is 28 lines; `ResolveInitInvocation` is 26 lines; `PrintInitFailureBanner` is 18 lines. All under 50.
- [x] A-047 No magic strings: `bannerLabelWorktree`/`bannerLabelRetry`/`bannerLabelRemove`/`bannerKeptNote`/`bannerOutputNote` defined as file-scoped consts (errors.go:67-72); `hintCommandNotOnPath`/`hintFileNotFound` defined at init.go:30-31. No literal banner labels in any other file (verified via grep).
- [x] A-048 Tests do not leak host side-effects: `runWt` defaults clear `TMUX`, `BYOBU_*`, `TERM_PROGRAM` (testutil_test.go:160-165) and sets `WT_TEST_NO_LAUNCH=1` (line 151). Tests using `--worktree-open=default` are now guarded by the seam.
- [x] A-049 `OpenInApp` short-circuits under `WT_TEST_NO_LAUNCH=1`: apps.go:201 checks `appCmd != "open_here" && os.Getenv("WT_TEST_NO_LAUNCH") == "1"` BEFORE the switch; emits `[wt-test-no-launch] would open %q in %q (repo=%q wt=%q)` and returns nil. `open_here` exemption verified.
- [x] A-050 `runWt` defaults `WT_TEST_NO_LAUNCH=1`: testutil_test.go:151 includes `"WT_TEST_NO_LAUNCH=1"` in the env passed to every wt invocation, before the user `env` parameter is appended (line 168) so user-env can override.
- [x] A-051 Default-app tests assert the seam: `TestCreate_WorktreeOpenDefault` (create_test.go:679) asserts `[wt-test-no-launch]` in stderr; `TestOpen_AppDefault` (open_test.go:196) asserts `[wt-test-no-launch]` when exit==0. Both fail loudly if a real launch leaks past the seam.

## Notes

- Check items as you review: `- [x]`
- All acceptance items must pass before `/fab-continue` (hydrate)
- If an item is not applicable, mark checked and prefix with **N/A**: `- [x] A-NNN **N/A**: {reason}`
